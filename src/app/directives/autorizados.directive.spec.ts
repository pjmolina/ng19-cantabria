import { AutorizadosDirective } from './autorizados.directive';

describe('AutorizadosDirective', () => {
  it('should create an instance', () => {
    const directive = new AutorizadosDirective();
    expect(directive).toBeTruthy();
  });
});
